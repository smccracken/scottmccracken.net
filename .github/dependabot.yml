version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    cooldown:
      semver-patch-days: 3
      semver-minor-days: 7
      semver-major-days: 10
    groups:
      patch-minor:
        update-types: ["patch", "minor"]
