<!doctype html>
<html lang="en" class="fonts">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">

		<meta property="og:site_name" content="{{ data.title }}">
		<link rel="canonical" href="{{ data.url }}">

		<title>{{ data.title }}</title>
		<meta name="description" content="{{ data.description }}">

		<meta name="application-name" content="{{ data.title }}">
		<link rel="manifest" href="/webappmanifest.json">
		
		<meta name="msapplication-TileImage" content="/_assets/img/icon-144.png">
		<meta name="theme-color" content="#6d252e">
		<link rel="icon" type="image/png" href="/_assets/img/icon-32.png" sizes="32x32">

		<!-- Half my site’s transfer is in `meta` nonsense. -->
		<script type="application/ld+json">
			{
				"@context": "http://schema.org",
				"@type": "Organization",
				"url": "{{ data.url }}",
				"title": "{{ data.title }}",
				"author_name": "{{ data.author.name }}",
				"logo": "{{ data.url }}/_assets/img/profile.gif",
				"sameAs" : [
					"https://twitter.com/scottmccracken"
				] 
			}
		</script>

		<meta name="twitter:card" content="summary_large_image">
		<meta name="twitter:site" content="@scottmccracken">
		<meta name="twitter:creator" content="@scottmccracken">
		<meta property="og:site_name" content="{{ data.name }}">
		<meta name="twitter:title" content="{{ data.title }}">
		<meta property="og:title" content="{{ data.title }}">
		<meta property="og:url" content="{{ data.url }}">
		<meta name="twitter:description" content="{{ lede }}">
		<meta property="og:description" content="{{ lede }}">
		<meta name="twitter:image" content="{{ data.url }}/_assets/img/profile.gif">
		<meta name="twitter:image:alt" content="{{ data.description }}">
		<meta property="og:image" content="{{ data.url }}/_assets/img/profile.gif">
		<meta property="og:image:alt" content="{{ data.description }}">

		<meta name="author" content="{{ data.author.name }}">

		<link rel="stylesheet" type="text/css" href="/_assets/css/screen.css">
	</head>
	<body>
		<a class="a11y-only" href="#who" title="Skip to main content">Skip to main content</a>
		<main role="main" id="main"{% if templateClass %} class="{{ templateClass }}"{% endif %}>
			<div class="body">
				<header role="banner">
					<h1 class="hed txt-shaded">{{ subhed | safe }}</h1>
					<p class="lede">{{ lede | safe }}</p>
				</header>

				<section class="sub sublede"{% if class == "home" %} id="who"{% endif %}>
					{{ layoutContent | safe }}
				</section>

				{% if class == "home" %}
				{% set sections = collections.sections | reverse %}
				{%- for section in sections-%}
					{% if section.data.pageClass == "contact" %}
					{% include "contact.njk" %}
					{% else %}
					{% include "section.njk" %}
					{% endif %}
				{%- endfor -%}
				{% endif %}
			</div>
		</main>

		<footer role="contentinfo">
			<p>Built using <a href="https://www.11ty.io/">Eleventy</a>. The source is available on <a href="https://github.com/smccracken/scottmccracken.net">GitHub</a>. Inspired by <a href="https://github.com/Wilto/hire.wil.to">wilto</a></p>
		</footer>

		<script async src="https://www.googletagmanager.com/gtag/js?id=UA-12417843-2"></script>
		<script>
		  window.dataLayer = window.dataLayer || [];
		  function gtag(){dataLayer.push(arguments);}
		  gtag('js', new Date());
		  gtag('config', 'UA-12417843-2');
		</script>
	</body>
</html>
